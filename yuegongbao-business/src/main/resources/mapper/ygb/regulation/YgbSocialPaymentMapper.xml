<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuegongbao.ygb.regulation.mapper.YgbSocialPaymentMapper">

    <resultMap type="YgbSocialPayment" id="YgbSocialPaymentResult">
        <id property="paymentId" column="payment_id" />
        <result property="statMonth" column="stat_month" />
        <result property="enterpriseId" column="enterprise_id" />
        <result property="enterpriseName" column="enterprise_name" />
        <result property="personId" column="person_id" />
        <result property="personName" column="person_name" />
        <result property="idCard" column="id_card" />
        <result property="regionCode" column="region_code" />
        <result property="baseAmount" column="base_amount" />
        <result property="paidAmount" column="paid_amount" />
        <result property="paymentStatus" column="payment_status" />
        <result property="sourceSerialNo" column="source_serial_no" />
        <result property="sourceStatus" column="source_status" />
        <result property="sourceMessage" column="source_message" />
        <result property="callbackTime" column="callback_time" />
        <result property="rawPayload" column="raw_payload" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="remark" column="remark" />
    </resultMap>

    <sql id="selectSocialPaymentVo">
        select payment_id, stat_month, enterprise_id, enterprise_name, person_id, person_name, id_card,
               region_code, base_amount, paid_amount, payment_status, source_serial_no, source_status,
               source_message, callback_time, raw_payload, create_by, create_time, update_by, update_time, remark
        from t_social_payment
    </sql>

    <select id="selectSocialPaymentList" parameterType="YgbSocialPayment" resultMap="YgbSocialPaymentResult">
        <include refid="selectSocialPaymentVo" />
        <where>
            del_flag = '0'
            <if test="statMonth != null and statMonth != ''">
                and stat_month = #{statMonth}
            </if>
            <if test="enterpriseId != null">
                and enterprise_id = #{enterpriseId}
            </if>
            <if test="personName != null and personName != ''">
                and person_name like concat('%', #{personName}, '%')
            </if>
            <if test="paymentStatus != null and paymentStatus != ''">
                and payment_status = #{paymentStatus}
            </if>
            <if test="regionCode != null and regionCode != ''">
                and region_code like concat(#{regionCode}, '%')
            </if>
            ${params.regionDataScope}
            ${params.enterpriseDataScope}
        </where>
        order by stat_month desc, enterprise_id asc, person_id asc, payment_id desc
    </select>

    <select id="selectSocialPaymentById" parameterType="Long" resultMap="YgbSocialPaymentResult">
        <include refid="selectSocialPaymentVo" />
        where payment_id = #{paymentId} and del_flag = '0'
    </select>

    <insert id="insertSocialPayment" parameterType="YgbSocialPayment" useGeneratedKeys="true" keyProperty="paymentId">
        insert into t_social_payment (
            stat_month, enterprise_id, enterprise_name, person_id, person_name, id_card, region_code,
            base_amount, paid_amount, payment_status, source_serial_no, source_status, source_message,
            callback_time, raw_payload, remark, create_by, create_time
        ) values (
            #{statMonth}, #{enterpriseId}, #{enterpriseName}, #{personId}, #{personName}, #{idCard}, #{regionCode},
            #{baseAmount}, #{paidAmount}, #{paymentStatus}, #{sourceSerialNo}, #{sourceStatus}, #{sourceMessage},
            #{callbackTime}, #{rawPayload}, #{remark}, #{createBy}, sysdate()
        )
    </insert>

    <delete id="deleteByScope" parameterType="YgbSocialPayment">
        delete from t_social_payment
        where stat_month = #{statMonth}
        <if test="enterpriseId != null">
            and enterprise_id = #{enterpriseId}
        </if>
        ${params.regionDataScope}
        ${params.enterpriseDataScope}
    </delete>

    <select id="selectWorkerSocialPaymentList" resultMap="YgbSocialPaymentResult">
        <include refid="selectSocialPaymentVo" />
        where del_flag = '0'
          and person_id = #{personId}
        <if test="yearPrefix != null and yearPrefix != ''">
            and stat_month like concat(#{yearPrefix}, '%')
        </if>
        order by stat_month desc, payment_id desc
    </select>

    <select id="selectWorkerSocialPaymentDetail" resultMap="YgbSocialPaymentResult">
        <include refid="selectSocialPaymentVo" />
        where del_flag = '0'
          and person_id = #{personId}
          and stat_month = #{statMonth}
        order by payment_id desc
        limit 1
    </select>

</mapper>
