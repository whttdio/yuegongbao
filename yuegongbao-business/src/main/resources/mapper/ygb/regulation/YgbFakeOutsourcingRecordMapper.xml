<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuegongbao.ygb.regulation.mapper.YgbFakeOutsourcingRecordMapper">

    <resultMap type="YgbFakeOutsourcingRecord" id="YgbFakeOutsourcingRecordResult">
        <id property="recordId" column="record_id" />
        <result property="statMonth" column="stat_month" />
        <result property="enterpriseId" column="enterprise_id" />
        <result property="enterpriseName" column="enterprise_name" />
        <result property="regionCode" column="region_code" />
        <result property="attendanceScore" column="attendance_score" />
        <result property="scheduleScore" column="schedule_score" />
        <result property="rewardScore" column="reward_score" />
        <result property="trainingScore" column="training_score" />
        <result property="totalScore" column="total_score" />
        <result property="suspectedFlag" column="suspected_flag" />
        <result property="warningStatus" column="warning_status" />
        <result property="evidenceSummary" column="evidence_summary" />
        <result property="remark" column="remark" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
    </resultMap>

    <sql id="selectFakeOutsourcingVo">
        select record_id, stat_month, enterprise_id, enterprise_name, region_code, attendance_score, schedule_score,
               reward_score, training_score, total_score, suspected_flag, warning_status, evidence_summary,
               remark, create_by, create_time, update_by, update_time
        from t_fake_outsourcing_record
    </sql>

    <select id="selectFakeOutsourcingList" parameterType="YgbFakeOutsourcingRecord"
        resultMap="YgbFakeOutsourcingRecordResult">
        <include refid="selectFakeOutsourcingVo" />
        <where>
            del_flag = '0'
            <if test="statMonth != null and statMonth != ''">
                and stat_month = #{statMonth}
            </if>
            <if test="enterpriseId != null">
                and enterprise_id = #{enterpriseId}
            </if>
            <if test="suspectedFlag != null and suspectedFlag != ''">
                and suspected_flag = #{suspectedFlag}
            </if>
            <if test="warningStatus != null and warningStatus != ''">
                and warning_status = #{warningStatus}
            </if>
            <if test="regionCode != null and regionCode != ''">
                and region_code like concat(#{regionCode}, '%')
            </if>
            ${params.regionDataScope}
        </where>
        order by stat_month desc, record_id desc
    </select>

    <insert id="insertFakeOutsourcingRecord" parameterType="YgbFakeOutsourcingRecord" useGeneratedKeys="true"
        keyProperty="recordId">
        insert into t_fake_outsourcing_record (
            stat_month, enterprise_id, enterprise_name, region_code, attendance_score, schedule_score, reward_score,
            training_score, total_score, suspected_flag, warning_status, evidence_summary, remark, create_by, create_time
        ) values (
            #{statMonth}, #{enterpriseId}, #{enterpriseName}, #{regionCode}, #{attendanceScore}, #{scheduleScore},
            #{rewardScore}, #{trainingScore}, #{totalScore}, #{suspectedFlag}, #{warningStatus}, #{evidenceSummary},
            #{remark}, #{createBy}, sysdate()
        )
    </insert>

</mapper>
