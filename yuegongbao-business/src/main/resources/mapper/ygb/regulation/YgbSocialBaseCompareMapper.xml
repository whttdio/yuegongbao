<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yuegongbao.ygb.regulation.mapper.YgbSocialBaseCompareMapper">

    <resultMap type="YgbSocialBaseCompare" id="YgbSocialBaseCompareResult">
        <id property="compareId" column="compare_id" />
        <result property="statMonth" column="stat_month" />
        <result property="enterpriseId" column="enterprise_id" />
        <result property="enterpriseName" column="enterprise_name" />
        <result property="personId" column="person_id" />
        <result property="personName" column="person_name" />
        <result property="idCard" column="id_card" />
        <result property="regionCode" column="region_code" />
        <result property="salaryAmount" column="salary_amount" />
        <result property="socialBaseAmount" column="social_base_amount" />
        <result property="diffRatio" column="diff_ratio" />
        <result property="compareResult" column="compare_result" />
        <result property="warningStatus" column="warning_status" />
        <result property="createBy" column="create_by" />
        <result property="createTime" column="create_time" />
        <result property="updateBy" column="update_by" />
        <result property="updateTime" column="update_time" />
        <result property="remark" column="remark" />
    </resultMap>

    <sql id="selectSocialBaseCompareVo">
        select compare_id, stat_month, enterprise_id, enterprise_name, person_id, person_name, id_card,
               region_code, salary_amount, social_base_amount, diff_ratio, compare_result, warning_status,
               create_by, create_time, update_by, update_time, remark
        from t_social_base_compare
    </sql>

    <select id="selectSocialBaseCompareList" parameterType="YgbSocialBaseCompare"
        resultMap="YgbSocialBaseCompareResult">
        <include refid="selectSocialBaseCompareVo" />
        <where>
            del_flag = '0'
            <if test="statMonth != null and statMonth != ''">
                and stat_month = #{statMonth}
            </if>
            <if test="enterpriseId != null">
                and enterprise_id = #{enterpriseId}
            </if>
            <if test="personName != null and personName != ''">
                and person_name like concat('%', #{personName}, '%')
            </if>
            <if test="compareResult != null and compareResult != ''">
                and compare_result = #{compareResult}
            </if>
            <if test="warningStatus != null and warningStatus != ''">
                and warning_status = #{warningStatus}
            </if>
            <if test="regionCode != null and regionCode != ''">
                and region_code like concat(#{regionCode}, '%')
            </if>
            ${params.regionDataScope}
        </where>
        order by stat_month desc, compare_id desc
    </select>

    <insert id="insertSocialBaseCompare" parameterType="YgbSocialBaseCompare" useGeneratedKeys="true"
        keyProperty="compareId">
        insert into t_social_base_compare (
            stat_month, enterprise_id, enterprise_name, person_id, person_name, id_card, region_code,
            salary_amount, social_base_amount, diff_ratio, compare_result, warning_status, remark, create_by, create_time
        ) values (
            #{statMonth}, #{enterpriseId}, #{enterpriseName}, #{personId}, #{personName}, #{idCard}, #{regionCode},
            #{salaryAmount}, #{socialBaseAmount}, #{diffRatio}, #{compareResult}, #{warningStatus}, #{remark},
            #{createBy}, sysdate()
        )
    </insert>

    <delete id="deleteByScope">
        delete from t_social_base_compare
        where stat_month = #{statMonth}
        <if test="enterpriseId != null">
            and enterprise_id = #{enterpriseId}
        </if>
    </delete>

</mapper>
