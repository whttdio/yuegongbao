# YGB Training Guide

## Operation Backoffice

- Use `operation/overview` to inspect enterprise review, message, job, and resume workloads.
- Use `jobReview` for岗位审核 and `resume` for简历管理.
- Use `message` records to maintain push and internal campaign tasks.

## Platform Governance

- Use `runtime/summary` for business-side runtime health.
- Use `document/exchange/securityAudit/backup` to maintain governance records and audit evidence.

## Report Team

- Generate new typed report codes from the existing unified report page without opening a second report module.
