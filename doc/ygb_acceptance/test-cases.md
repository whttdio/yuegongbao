# YGB Test Cases

1. Generate each new typed report code and verify list/detail/export paths return data.
2. Query `operation/overview` and confirm enterprise review, message, job, and resume counts return.
3. Query `operation/jobReview` and `operation/resume` list/detail/export.
4. Query `platform/runtime/summary` and verify server/log counts are present.
5. Query each platform governance submodule CRUD route with `YgbModuleRecord`.
6. Verify public portal home payload contains the five new content sections.
