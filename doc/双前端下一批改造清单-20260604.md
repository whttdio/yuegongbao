# 双前端下一批改造清单

## 本轮已核对

### 1. 门户首页入口

- 两个门户都走 `/index`
- 且 [index.vue](/D:/javaproject/yuegongbao/YueGongBao-Vue/yuegongbao-ui/src/views/index.vue) 已按当前门户动态渲染不同工作台
- 首页快捷入口已按“当前门户 + 当前账号可见路由”动态收口
- 首页模块索引已兼容 `ygb/xxx/index` 路径结构，快捷入口不会再因为菜单尾段统一为 `index` 而丢失
- 首页已接入真实摘要接口并按权限并行加载，不再只是静态门户说明
- `ygb` 首页已补合同/考勤/工资办理链摘要，企业管理员、操作员、财务经办的快捷入口开始显示真实待办文案
- `ygb` 首页已补社保缴费、社保基数比对、税务比对摘要，财务闭环入口开始显示真实风险计数
- 企业角色进入 `ygb` 与 `azb` 时，首页推荐路径已明显不同
- 门户品牌与首页文案来自 [portal.js](/D:/javaproject/yuegongbao/YueGongBao-Vue/yuegongbao-ui/src/config/portal.js)
- 路由过滤由 [permission.js](/D:/javaproject/yuegongbao/YueGongBao-Vue/yuegongbao-ui/src/store/modules/permission.js) + [portal.js](/D:/javaproject/yuegongbao/YueGongBao-Vue/yuegongbao-ui/src/utils/portal.js) 负责
- 门户切换后首页、导航栏、Logo 已能随当前路由响应式切换，不再保留旧门户外壳

结论：

- “双门户默认首页未分开” 不是当前阻塞项
- 首页壳层、真实聚合和角色入口已进入可用状态，下一步应继续补更细的办理型待办与重点对象聚合

### 2. 本轮新增的门户级摘要接口

- `GET /ygb/heightWork/report/summary`
- `GET /ygb/device/summary`
- `GET /ygb/cockpit/ygb/dashboard`
- `GET /ygb/cockpit/azb/dashboard`
- `GET /ygb/aqInsurance/summary`
- `GET /ygb/injury/event/summary`
- `GET /ygb/report/summary`
- `GET /ygb/person/summary`
- `GET /ygb/enterprise/summary`
- `GET /ygb/credit/score/summary`
- `GET /ygb/injury/prevention/summary`
- `GET /ygb/preventionFund/summary`
- `GET /ygb/expansion/uninsured/summary`
- `GET /ygb/warning/rule/summary`
- `GET /ygb/aiReport/config/summary`

补充进展：

- `ygb/contract` 已升级为粤工保合同备案办理台账页面，已补 `getContractSummary` 摘要、办理承接焦点、当前选中合同和详情办理提示
- `ygb/attendanceRaw` 已升级为粤工保考勤上报办理台账页面，已补 `getAttendanceRawSummary` 摘要、办理承接焦点、当前选中考勤对象和详情办理提示
- `ygb/socialPayment` 已升级为粤工保社保缴费整改台账页面，已补 `getSocialPaymentSummary` 摘要、办理承接焦点、当前选中缴费对象和详情办理提示
- `ygb/socialBaseCompare` 已升级为粤工保社保基数比对整改台账页面，已补 `getSocialBaseCompareSummary` 摘要、办理承接焦点、当前选中比对对象和详情办理提示
- `ygb/taxCompare` 已升级为粤工保个税比对整改台账页面，已补 `getTaxCompareSummary` 摘要、办理承接焦点、当前选中比对对象和详情办理提示
- `ygb/employmentRatio` 已升级为粤工保用工比例合规整改台账页面，已补 `getEmploymentRatioSummary` 摘要、办理承接焦点、当前选中治理对象和详情办理提示
- `ygb/fakeOutsourcing` 已升级为粤工保假外包识别与复核台账页面，已补 `getFakeOutsourcingSummary` 摘要、办理承接焦点、当前选中识别对象和详情办理提示
- `ygb/newformWorker` 已升级为粤工保新业态人员监管台账页面，已补 `getNewformWorkerSummary` 摘要、办理承接焦点、当前选中监管对象和详情办理提示
- `ygb/occupationMonitor` 已升级为粤工保职业病监测台账页面，已补 `getOccupationMonitorSummary` 摘要、办理承接焦点、当前选中监管对象和详情办理提示
- `ygb/salaryBatch` 已升级为粤工保工资批次办理台账页面，已补 `getSalaryBatchSummary` 摘要、办理承接焦点、当前选中批次和详情办理提示
- `ygb/statReport` 已升级为粤工保门户语义的统计报表页面，已补 `getStatReportSummary` 摘要、推荐月报入口和当前归档焦点
- `ygb/enterprise` 已升级为粤工保企业主数据办理台账页面，已补 `getEnterpriseSummary` 摘要、办理承接焦点、当前选中企业和详情办理提示
- `ygb/person` 已升级为粤工保人员主数据办理台账页面，已补 `getPersonSummary` 摘要、办理承接焦点、当前选中人员和详情办理提示
- `ygb/warning` 已升级为粤工保联动预警办理闭环页面，已补 `getWarningSummary` 摘要、办理承接焦点、当前选中工单和详情办理提示
- `ygb/heightWorkReport` 已升级为粤工保高处作业报备办理台账页面，已补 `getHeightWorkReportSummary` 摘要、办理承接焦点、当前选中报备和详情办理提示
- `ygb/creditScore` 已升级为粤工保企业信用评分办理台账页面，已补 `getCreditScoreSummary` 摘要、办理承接焦点、当前选中企业和详情办理提示
- `ygb/injuryEvent` 已升级为粤工保工伤事件办理闭环台账页面，已补 `getInjuryEventSummary` 摘要、办理承接焦点、当前选中事件和详情办理提示
- `ygb/device` 已升级为粤工保设备接入与授权办理台账页面，已补 `getDeviceSummary` 摘要、办理承接焦点、当前选中设备和日志联动提示
- `ygb/aqInsurance` 已升级为粤工保投保监管办理台账页面，已补 `getAqInsuranceSummary` 摘要、办理承接焦点、当前选中保单和详情办理提示
- `ygb/preventionFund` 已升级为粤工保事故预防资金结算台账页面，已补 `getPreventionFundSummary` 摘要、办理承接焦点、当前选中资金池和详情办理提示
- `ygb/preventionProject` 已升级为粤工保预防项目执行办理台账页面，已补 `getPreventionProjectSummary` 摘要、办理承接焦点、当前选中项目和详情办理提示
- `ygb/uninsuredList` 已升级为粤工保漏保整改办理台账页面，已补 `getUninsuredListSummary` 摘要、办理承接焦点、当前选中对象和详情办理提示
- `ygb/warningRule` 已升级为粤工保联动预警规则办理台账页面，已补 `getWarningRuleSummary` 摘要、办理承接焦点、当前选中规则和详情办理提示
- `ygb/aiReportConfig` 已升级为粤工保评分模型配置办理台账页面，已补 `getAiReportConfigSummary` 摘要、办理承接焦点、当前选中配置和详情办理提示
- `ygb/aiReport` 已在原有复杂 AI 工作台基础上补齐粤工保办理台账语义，新增摘要卡、办理承接焦点、当前选中报告、办理路径和详情办理提示，保留图表、排名、风险对象和预览导出能力

这些接口已经覆盖：

- 驾驶舱总览
- 高处作业治理摘要
- 设备治理摘要
- 投保监管摘要
- 工伤事件摘要
- 报表摘要
- 人员/企业主数据摘要
- 信用评分摘要
- 预防项目与资金摘要
- 漏保整改摘要
- 规则治理摘要
- AI 模型配置摘要

## 下一批优先级

### P0

1. `azb key object aggregation`
   - 进展：`azb` 首页已从“预警 / 设备 / 高处作业”三组重点队列扩到按角色聚合的 `风险保单 / 低余额资金池 / 红码信用对象 / 可查看区域报表`
   - 结果：`108 ygb_insurer`、`109 ygb_bank` 进入 `azb` 时，不再只看到监管向摘要，可直接从首页下钻保险、资金、信用和报表对象
   - 目标：补安责保首页重点对象和快捷入口聚合，继续减少二次跳转

2. `ygb finance/workflow aggregation`
   - 进展：`ygb/contract`、`ygb/salaryBatch` 已从基础 CRUD 升级为办理台账，合同和工资不再只依赖首页摘要，页面内已补摘要、焦点对象、流程路径和详情提示
   - 进展补充：`ygb/taxCompare` 已从基础列表升级为个税比对整改台账，补齐摘要卡、办理承接焦点、当前选中对象、整改路径和详情抽屉提示
   - 进展补充：`ygb/cockpit` 已升级为“企业办理与监管总览工作台”，并接入 `GET /ygb/cockpit/ygb/dashboard` 聚合接口
   - 进展补充：`azb/cockpit` 已升级为按 `107/108/109` 分层的“风险治理与协同工作台”，并接入 `GET /ygb/cockpit/azb/dashboard` 角色聚合字段，已通过 `vite build --mode azb`
   - 目标：继续补首页聚合，以及 `ygb` 主链页面更细的企业维度、月份维度和角色待办优先级聚合

3. `home backend aggregator optionalization`
   - 目标：评估是否需要把当前前端并行聚合进一步沉淀成统一首页聚合接口
   - 原则：仅在请求碎片和权限判断复杂度继续上升时再落地，避免过早复制已有摘要能力

- `read-only role lightweight views` 已继续扩面：`yuegongbao-ui/src/utils/roleView.js` 已修正角色提示文案，并在 `azb/aqInsurance`、`azb/creditScore`、`azb/statReport`、`azb/preventionFund`、`azb/uninsuredList`、`azb/aiReport`、`azb/enterprise`、`azb/person`、`azb/warning`、`azb/device`、`azb/heightWorkReport`、`azb/cockpit`、`azb/injuryEvent`、`azb/warningRule`、`azb/aiReportConfig` 前端明确收口只读角色的生成、同步、重生成、处置、对象台账维护、设备远程指令、高处作业闭环、工伤流转、规则维护和 AI 模型版本维护动作
- `azb-only portal normalization` 已补：`107/108/109` 这类安责保专属角色在持 Token 进入 `ygb` 门户时，会在前端自动归位到 `azb`，先从使用路径上避免误入粤工保壳层

### P1

1. `read-only role lightweight views`
   - 现状：`azb/aqInsurance / azb/creditScore / azb/statReport / azb/preventionFund / azb/uninsuredList / azb/aiReport / azb/enterprise / azb/person / azb/warning / azb/device / azb/heightWorkReport / azb/cockpit / azb/injuryEvent / azb/warningRule / azb/aiReportConfig` 已切入只读模式，页面会显式提示“只读协同/只读看板”，并隐藏生成、同步、重生成、处置、对象维护、设备指令、高处作业维护、工伤流转、规则维护和 AI 模型版本入口
   - 下一步：结合目标库核对 `portal_scope + 角色菜单矩阵`，确认只读角色是否只拿到查询/导出类权限
2. `portal copy cleanup`
3. `detail summary differentiation follow-up`
4. `detail summary differentiation`
   - 目标：继续把人员、预警、高处作业、信用评分、工伤事件、设备、安责险、资金池等共性模块详情页补成 `ygb/azb` 两套摘要语义，而不是只靠顶部说明区分
   - 现状：`enterprise / person / warning / heightWorkReport / creditScore / injuryEvent / device / aqInsurance / preventionFund / preventionProject / uninsuredList / warningRule / aiReport / aiReportConfig` 已完成，下一块优先切 `read-only role lightweight views`

## 角色与菜单边界待验项

以下内容代码侧已有基础能力，但仍需结合目标库数据验证：

前置说明：
- 当前 `sys_role_menu` 仍是全量角色菜单关系表，没有“按门户分别存授权”的结构；现阶段先靠 `portal_scope + 前端门户归位` 收口 `107/108/109` 的实际使用路径

1. `portal_scope` 是否已经覆盖所有 `ygb` 独占按钮
2. `107/108/109` 是否只在 `azb` 看到应急侧菜单
3. `104/105` 在 `azb` 是否只保留现场治理相关入口
4. `108/109` 是否没有新增、修改、结束类按钮

## 建议执行顺序

1. 先做一轮 SQL/目标库的角色菜单注入核对
2. 再补双门户首页剩余真实聚合数据
3. 最后继续做门户文案收口与只读角色瘦身补漏
